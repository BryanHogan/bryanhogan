---

---

<form
    action="https://buttondown.email/api/emails/embed-subscribe/BryanHogan"
    method="post"
    target="popupwindow"
    onsubmit="window.open('https://buttondown.email/BryanHogan', 'popupwindow')"
    class="buttondown-form"
>
    <h1 class="text-shadow">Get Updates</h1>
    <label for="bd-email" class="text-shadow text-align-center"
        ><p>Get new posts delivered to your inbox.<br/>Secure and zero spam.<br/>Enter your email.</p></label
    >
    <input type="email" name="email" id="bd-email" placeholder="Your email" class="email"/>
    <input type="hidden" name="tag" value="blog" />
    <input type="submit" value="Subscribe" class="button" />
</form>
<style>
    .buttondown-form {
        display: flex;
        justify-content: center;
        flex-direction: column;
        & >* {
            margin-top: 1em;
        }
        & h1 {
            text-align: center;
        }
        input {
            width: 80%;
            border: 2px solid var(--color-main-bright);
            border-radius: 5px;
            padding: 4px;
            box-shadow: var(--box-shadow-medium);
            font-size: 1.2rem;
            margin-inline: auto;
        }
        & .email {
            margin-top: 2em;
        }
        & .button {
            color: var(--color-main-dark);
            background-color: #fff;
            border: 0px;
            border-radius: 10px;
            box-shadow: var(--box-shadow-medium);
            width: 30%;
            min-width: 100px;
            font-size: 1rem;
            margin-bottom: 50px;
            padding: 10px;
            margin-inline: auto;
            transition: all 200ms linear;
            box-shadow:
                var(--box-shadow-large),
                inset 0px 0px 0px 0px rgba(0, 0, 0, 0.25);
            &:hover {
                box-shadow:
                    0px 0px 0px 0px rgba(0, 0, 0, 0.25),
                    inset var(--box-shadow-large);
                text-shadow: var(--text-shadow-inset);
            }
        }
    }
</style>
